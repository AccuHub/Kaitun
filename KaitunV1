local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "accuNotify"
ScreenGui.Parent = PlayerGui
ScreenGui.IgnoreGuiInset = true
ScreenGui.DisplayOrder = 999

local Blur = Instance.new("BlurEffect")
Blur.Size = 0
Blur.Parent = Lighting

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Parent = ScreenGui
Main.Size = UDim2.new(1, 0, 1, 0)
Main.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Main.BackgroundTransparency = 1
Main.BorderSizePixel = 0
Main.ZIndex = 1

local Content = Instance.new("Frame")
Content.Name = "Content"
Content.Parent = Main
Content.Size = UDim2.new(0, 550, 0, 350)
Content.Position = UDim2.new(0.5, 0, 0.5, 0)
Content.AnchorPoint = Vector2.new(0.5, 0.5)
Content.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
Content.BackgroundTransparency = 0.1
Content.BorderSizePixel = 0
Content.ZIndex = 2

local ContentCorner = Instance.new("UICorner")
ContentCorner.CornerRadius = UDim.new(0, 15)
ContentCorner.Parent = Content

local ContentStroke = Instance.new("UIStroke")
ContentStroke.Thickness = 2
ContentStroke.Color = Color3.fromRGB(255, 255, 255)
ContentStroke.Transparency = 0.8
ContentStroke.Parent = Content

local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Parent = Content
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
CloseBtn.Position = UDim2.new(1, -40, 0, 15)
CloseBtn.Size = UDim2.new(0, 25, 0, 25)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 14
CloseBtn.ZIndex = 10

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 5)
CloseCorner.Parent = CloseBtn

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = Content
Title.Size = UDim2.new(1, -40, 0, 80)
Title.Position = UDim2.new(0, 20, 0, 45)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.GothamBold
Title.Text = "Script Này Đã Không Còn Sử Dụng Được Nữa!!"
Title.TextColor3 = Color3.fromRGB(255, 65, 65)
Title.TextSize = 28
Title.TextWrapped = true
Title.ZIndex = 5

local UIStrokeTitle = Instance.new("UIStroke")
UIStrokeTitle.Thickness = 3
UIStrokeTitle.Color = Color3.fromRGB(0, 0, 0)
UIStrokeTitle.Transparency = 0.4
UIStrokeTitle.Parent = Title

local Desc = Instance.new("TextLabel")
Desc.Name = "Desc"
Desc.Parent = Content
Desc.Position = UDim2.new(0, 25, 0, 135)
Desc.Size = UDim2.new(1, -50, 0, 80)
Desc.BackgroundTransparency = 1
Desc.Font = Enum.Font.GothamMedium
Desc.Text = "Hãy Vào Discord Của Chúng Tôi Để Lấy Bản Kaitun Sailor Piece Mới Nhất!"
Desc.TextColor3 = Color3.fromRGB(230, 230, 230)
Desc.TextSize = 18
Desc.TextWrapped = true
Desc.ZIndex = 5

local Btn = Instance.new("TextButton")
Btn.Name = "CopyBtn"
Btn.Parent = Content
Btn.Position = UDim2.new(0.5, 0, 0, 255)
Btn.Size = UDim2.new(0, 280, 0, 55)
Btn.AnchorPoint = Vector2.new(0.5, 0)
Btn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
Btn.Font = Enum.Font.GothamBold
Btn.Text = "Discord: https://discord.gg/f66raEgAZp"
Btn.TextColor3 = Color3.fromRGB(255, 255, 255)
Btn.TextSize = 17
Btn.AutoButtonColor = false
Btn.ZIndex = 5

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 12)
BtnCorner.Parent = Btn

local UIStrokeBtn = Instance.new("UIStroke")
UIStrokeBtn.Color = Color3.fromRGB(255, 255, 255)
UIStrokeBtn.Transparency = 0.7
UIStrokeBtn.Thickness = 2
UIStrokeBtn.Parent = Btn

Btn.MouseButton1Click:Connect(function()
	pcall(function()
		setclipboard("https://discord.gg/f66raEgAZp")
	end)
	Btn.Text = "Discord: https://discord.gg/f66raEgAZp"
	Btn.BackgroundColor3 = Color3.fromRGB(67, 181, 129)
	task.wait(1.5)
	Btn.Text = "Discord: https://discord.gg/f66raEgAZp"
	Btn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
end)

CloseBtn.MouseButton1Click:Connect(function()
	TweenService:Create(Main, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
	TweenService:Create(Content, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play()
	TweenService:Create(Blur, TweenInfo.new(0.5), {Size = 0}):Play()
	task.wait(0.5)
	ScreenGui:Destroy()
	Blur:Destroy()
end)

TweenService:Create(Main, TweenInfo.new(1), {BackgroundTransparency = 0.3}):Play()
TweenService:Create(Blur, TweenInfo.new(1), {Size = 25}):Play()

Btn.MouseEnter:Connect(function()
	TweenService:Create(Btn, TweenInfo.new(0.3), {Size = UDim2.new(0, 300, 0, 60), BackgroundColor3 = Color3.fromRGB(100, 115, 255)}):Play()
end)

Btn.MouseLeave:Connect(function()
	TweenService:Create(Btn, TweenInfo.new(0.3), {Size = UDim2.new(0, 280, 0, 55), BackgroundColor3 = Color3.fromRGB(88, 101, 242)}):Play()
end)
